/*
For loops cubes of numbers
Author: Enoshmaxwell
Reg No:D33-2453-2023
Date: 5th March 2025
*/

#include <stdio.h>

int main() {
    int start, stop, i;

    printf("Enter a starting value: \n");
    scanf("%d", &start);

    printf("Enter a stopping value:\n");
    scanf("%d", &stop);

        for (i = start; i <= stop; i++) {
            printf("Number: %d, Cube: %d\n", i, i * i * i);
        }
    

    return 0; // Successful execution
}
